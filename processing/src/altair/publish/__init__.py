"""Verifier and publisher (SPEC §6.6): checks a job's outputs, registers them
as blobs on the NAS (and for S3), writes sidecars, viewing copies and
previews, updates the catalog and reports to the Hub."""
